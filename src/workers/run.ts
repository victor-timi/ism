/**
 * Worker dispatcher — entrypoint for cron jobs.
 * Run with: pnpm worker
 *
 * This will be implemented in Milestone 2 to:
 * 1. Check which adapters are due to run
 * 2. Execute them sequentially
 * 3. Record results in IngestionRun table
 */

console.log("Worker dispatcher started");
console.log("No adapters configured yet — this will be set up in Milestone 2");
process.exit(0);
